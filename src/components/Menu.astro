---
const links = [
    { name: 'GET STARTED', href: '/GetStarted'},
    /*{ name: 'NEWS', href: '/news' },*/
    { name: 'DOCS', href: 'https://docs.warthog.network/', target: '_blank'},
    { name: 'WHITEPAPER', href: '/Whitepaper'},
    { name: 'FAQS', href: '/faqs'},
    { name: 'EXPLORER', href: 'https://wartscan.io/', target: '_blank'}
];
---

    <style>
.menu{
    height: 2.5rem;
}

.underlineText{
  
  justify-content: center;
   border-radius: 4rem;
   
   padding: .5rem;
 }
  
  .underlineText2{  
    font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
    border-radius: 4rem;
    position: relative;
  }

  .underlineText2:after {
    
    content: '';
   position: absolute;
   width: 100%;
   height: 0.01rem;
   left: 0;
   bottom: 0;
   background:yellow;
  }

  .drawline::after{
    transform: scale(0,1);
    transition: transform 0.3s ease;
    transform-origin: 0% 100%;
  }

  .drawline::after{
    transform: scale(0,1);
    transition: transform 0.3s ease;
    transform-origin: 0% 100%;
  }

  .drawline:hover::after{
   transform: scale(1,1);
  }

  @media screen and (min-width: 1024px) {

  .menu {
    margin-left: 5em;
  }

  .menu a {
    display: inline-block;
    padding: .5rem;
  }
}
    </style>
    <div class='menu'>
    {links.map((link) => (
            <a class="underlineText"  target={link.target} href={link.href}>
                <p class="underlineText2 drawline">{link.name}</p>
            </a>
    ))}
    </div>
